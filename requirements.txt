# =============================================================================
# TARS -- Python Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# =============================================================================

# ---------------------------------------------------------------------------
# Phase 1 -- Mission Foundation
# ---------------------------------------------------------------------------

# MAVSDK -- Python SDK for MAVLink drone communication
# This is how your Python code talks to PX4 autopilot
# It uses gRPC internally to communicate with a MAVSDK server process
mavsdk>=2.0.0

# Pydantic -- Data validation and JSON serialization
# We use this to define strict telemetry data models
# Ensures every telemetry snapshot has the right structure
pydantic>=2.0.0

# python-dotenv -- Load .env files into environment variables
# Reads PX4_CONNECTION, TELEMETRY_RATE_HZ, etc. from .env file
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# Phase 2 -- Mission Replay System
# ---------------------------------------------------------------------------

# FastAPI -- Modern async web framework for the replay API
fastapi>=0.115.0

# Uvicorn -- ASGI server to run FastAPI
uvicorn[standard]>=0.30.0

# SQLAlchemy -- Async ORM for PostgreSQL
sqlalchemy[asyncio]>=2.0.0

# asyncpg -- Async PostgreSQL driver (used by SQLAlchemy async engine)
asyncpg>=0.29.0

# psycopg2-binary -- Sync PostgreSQL driver (used by Alembic migrations)
psycopg2-binary>=2.9.0

# Alembic -- Database migration tool for SQLAlchemy
alembic>=1.13.0

# httpx -- Async HTTP client (used for testing FastAPI and Phase 3 replay client)
httpx>=0.27.0

# pytest + pytest-asyncio -- Testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0

# ---------------------------------------------------------------------------
# Phase 3 -- State Engine
# ---------------------------------------------------------------------------

# Redis -- Async client for operational state store
redis>=5.0.0

# ---------------------------------------------------------------------------
# Phase 5 -- Gemini Reasoning Layer
# ---------------------------------------------------------------------------

# Google ADK -- Agent Development Kit for Gemini integration
google-adk>=1.0.0

# Google GenAI -- Gemini SDK types and client
google-genai>=1.0.0

# ---------------------------------------------------------------------------
# Phase 6 -- Phoenix Integration (Observability)
# ---------------------------------------------------------------------------

# OpenTelemetry -- Distributed tracing framework
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0

# OTLP HTTP exporter -- Exports spans to Phoenix via OTLP/HTTP
opentelemetry-exporter-otlp-proto-http>=1.20.0

# arize-phoenix -- Phoenix observability platform (optional, for local server)
# Install separately if running Phoenix locally:
#   pip install arize-phoenix

# ---------------------------------------------------------------------------
# Phase 7 -- Neo4j Operational Memory
# ---------------------------------------------------------------------------

# Neo4j -- Official async Python driver for Neo4j graph database
neo4j>=5.0.0
